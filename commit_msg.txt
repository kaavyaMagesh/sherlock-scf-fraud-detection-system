fix: Layer 2 backend + frontend-backend connection fixes

Backend changes:
- Added GRN supplier/buyer ID validation to checkTripleMatch
- Fixed fuzzy duplicate detection: exact amount match + cross-lender scope
- Equalised penalty weights (PO and GRN both 40pts)
- Added mandatory 3-document enforcement (Invoice → PO → GRN)
- Added invoiceNumber to invoice presence check in checkTripleMatch

Frontend changes:
- Wired Single Invoice Entry form to correct POST /api/invoices endpoint
- Replaced mock toasts in action-panel.tsx with real disburse/override API calls
- Added error state rendering to invoice-queue.tsx side panel
- Updated useInvoiceDetail hook to expose backend error messages
- Added unused backend fields to Risk Analysis panel (invoice_date, supplier_id, buyer_id, expected_payment_date)
